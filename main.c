#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int flag=1;
	int y; //y를 main의 지역변수로 만들어주면 컴파일에러 발생안함. 
	while (flag!=0)
	{

		y=3;
		flag=0;
	}
	y=4;
	return 0;
}
